# Clinical Trial Optimization Environment — Dependencies
# Used by Dockerfile and local setup

# OpenEnv framework (core library — provides Environment, Action, Observation, State base classes)
openenv-core>=0.2.1

# Web framework (auto-installed by openenv-core, pinned for reproducibility)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# OpenAI-compatible client for inference script
openai>=1.20.0

# HTTP client for client.py
requests>=2.31.0

# Utility
python-dotenv>=1.0.0
